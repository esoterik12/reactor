import { levelSelectionInfo } from './contentInfo'

export const spotItInfo = {
  levelSelectionInfo,
  primaryInputInfo: {
    title: 'Input words',
    inputInfo:
      'Provide a comma-separated list of words. Each round, one will randomly be selected to appear twice in the game.'
  }
}
